[project]
name = "pg-attn-perf"
version = "0.1.0"
description = "PagedAttention performance benchmarking"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "vllm",
    "transformers",
    "nvidia-ml-py",
    "pandas",
    "pyyaml",
    "python-dotenv>=1.2.2",
    "accelerate>=1.13.0",
    "jupyter>=1.1.1",
    "ipykernel>=7.2.0",
    "notebook>=7.5.6",
    "matplotlib>=3.10.9",
]
